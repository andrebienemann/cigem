from setuptools import setup, Extension


setup(
    name="{{ package.name }}",
    version="0.1.0",
    packages=["{{ package.name }}"],
    ext_package="{{ package.name }}",
    ext_modules=[
        
    ],
)
